<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List - Landing</title>
    <style>
        /* Reset dasar */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        /* Pengaturan layout dasar */
        body {
           
            align-items: center;
            justify-content: center;
            background-color: #fff;
        }

         /* Header styling */
        header {
            width: 100%;
            max-width: 2000px; 
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px ;
            margin: 20px auto; 
            font-size: 2em; /* Ukuran font untuk judul */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }


        .container {
            display: flex;
            width: 100%;
            height: 100vh; /* Menambahkan tinggi penuh layar */
            background-color: #fff;
            overflow: hidden;
        }

        /* Kolom kiri untuk teks sambutan */
        .content-left {
            flex: 1;
            padding: 50px;
            background-color: #ffffff;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .content-left h2 {
            font-size: 1em;
            color: #FF6347;
            letter-spacing: 2px;
            margin-bottom: 15px;
            text-transform: uppercase;
        }

        .content-left h1 {
            font-size: 2.5em;
            color: #333;
            margin-bottom: 20px;
        }

        .buttons {
            display: flex;
            gap: 20px;
        }

        .buttons a {
            display: inline-block;
            padding: 12px 24px;
            font-size: 1em;
            text-decoration: none;
            color: white;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .buttons a.login {
            background-color: #333;
        }

        .buttons a.login:hover {
            background-color: #555;
        }

        .buttons a.register {
            background-color: transparent;
            border: 2px solid #333;
            color: #333;
        }

        .buttons a.register:hover {
            background-color: #333;
            color: white;
        }


    </style>
</head>
<body>

    <header>
        SmartTask
    </header>

    <div class="container">
        <!-- Konten Kiri -->
        <div class="content-left">
            <h2>Hello, Welcome to SmartTask</h2>
            <h1>Be Stay Organized and Productive</h1>
            <div class="buttons">
                <a href="/auth/login" class="login">Login</a>
                <a href="/auth/register" class="register">Register</a>
            </div>
        </div>
    </div>
</body>
</html>
